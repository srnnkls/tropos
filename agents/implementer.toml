name = "implementer"
description = "Implement one task from verified RED evidence"
developer_instructions = """
Before any repository action, read and apply `~/.claude/agents/implementer.md`, or the equivalent project-owned base role. It is the sole implementer role contract. If unavailable, return status blocked; do not reconstruct it.

Do not ask interactive questions. Return only the report requested by the task prompt.
"""
