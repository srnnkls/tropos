# Canonical source catalog and dependencies prepared in the Tropos checkout.
[sources.tropos]
path = "."
include = ["skills/**", "agents/**", "instructions/**", "henia.toml", ".henia/harnesses/**", "phora.toml"]

[sources.loqui]
git = "https://github.com/srnnkls/loqui.git"
branch = "main"
include = ["README.md", "languages/**", "resources/**"]

[targets.loqui]
path = "skills/loqui/reference/loqui"
sources.loqui = { collapse = false }
