# Review Models

Read live identities and capabilities through `peer list`. The canonical [peer routing contract](../../peer/reference/routing.md) owns registry fields, host compatibility, native/routable/external dispatch, generated variants, and effort semantics.

Review operations choose roles and required execution classes; they never hardcode a model or infer a mechanism from an alias.
